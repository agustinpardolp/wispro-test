import styled from "styled-components";

const StyledWrapper = styled.div`

`;

const StyledTitle = styled.h2`
  border-bottom: 1px solid #4e4b4b4d;
  color: var(--primary-titles);
  font-size: 1.5rem;
  margin: 10px;
  padding: 10px;
`;

export { StyledWrapper, StyledTitle };
